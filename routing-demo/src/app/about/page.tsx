// static metadata
export const metadata = {
   // takes precidence if both('metadata' in 'layout.tsx') are present
   title: 'About next',
};

export default function About() {
   return <div>About me</div>;
}
